{
  "version": 2,
  "buildCommand": "echo 'Build starting...' && pwd && ls -la && if [ ! -d client ]; then echo 'ERROR: client directory missing!' && git ls-files client/ | head -10 && exit 1; fi && echo 'Client directory found' && cd client && npm install && npm run build && cd .. && mkdir -p public && cp -r client/build/* public/ && echo 'Build complete!'",
  "installCommand": "npm install",
  "outputDirectory": "public",
  "functions": {
    "api/index.js": {
      "runtime": "@vercel/node",
      "includeFiles": "server/**"
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/index.js"
    },
    {
      "src": "/(.*)",
      "dest": "api/index.js"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}

